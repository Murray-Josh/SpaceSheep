import pygame
import modules

# Initiates pygame to be used throughout the program
pygame.init()

# Creates the pygame screen and defaults it to running in full screen mode
pygame.display.set_mode((0, 0), pygame.FULLSCREEN)

modules.menufunction()
